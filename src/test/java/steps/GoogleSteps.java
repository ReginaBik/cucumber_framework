package steps;

import org.junit.Before;
import org.openqa.selenium.WebDriver;
import utils.Driver;

public class GoogleSteps {
    WebDriver driver;

    @Before
    public void setup(){
        driver = Driver.getDriver();
    }

    //We will be writing out tests here


}
